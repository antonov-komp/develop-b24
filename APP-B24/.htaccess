# Защита конфигурационных файлов от прямого доступа
<FilesMatch "^(config\.json|settings\.json|settings\.php)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Защита логов от прямого доступа
<FilesMatch "\.log$">
    Order allow,deny
    Deny from all
</FilesMatch>



